const SECONDS = 1000;
const MINUTS = SECONDS * 60;
const HOURS = MINUTS * 60;
const DAYS = HOURS * 24;
const REFRASH_RATE = 5;

module.exports = {
  SECONDS,
  MINUTS,
  HOURS,
  DAYS,
  REFRASH_RATE
};
